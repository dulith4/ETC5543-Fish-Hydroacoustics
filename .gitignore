# History files
.Rhistory
.Rapp.history

# Session Data files
.RData
.RDataTmp

# User-specific files
.Ruserdata

# Example code in package build process
*-Ex.R

# Output files from R CMD build
/*.tar.gz

# Output files from R CMD check
/*.Rcheck/

# RStudio files
.Rproj.user/

# produced vignettes
vignettes/*.html
vignettes/*.pdf

# OAuth2 token, see https://github.com/hadley/httr/releases/tag/v0.3
.httr-oauth

# knitr and R markdown default cache directories
*_cache/
/cache/

# Temporary files created by R markdown
*.utf8.md
*.knit.md

# R Environment Variables
.Renviron

# pkgdown site
docs/

# translation temp files
po/*~

# RStudio Connect folder
rsconnect/

# --- outputs policy ---
# ignore local caches only
outputs/cache/

# keep the folder around in fresh clones
!outputs/.gitkeep

# Derived feature table (rebuild via Analysis/02_feature_engineering.R)
outputs/tables/reg_freq_quintiles_wide.rds
outputs/tables/fish_freq_quintiles_long.rds

# === Ignore derived outputs (predictions, leaderboards, models) ===

# Ignore model binaries
outputs/models/**

# Ignore only timestamped CSV/RDS files (e.g. *_20250904_162038.csv)
outputs/tables/*_[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]_*.csv
outputs/tables/*_[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]_*.rds

# Backscatter-transformed splits (rebuild via Analysis/02a_check_transformations.R)
outputs/tables/*backscatter_450*.rds
outputs/tables/*backscatter_450*.csv

# RNN artifacts (confusion matrix, history, metrics, etc.)
outputs/tables/rnn_confusion_*.csv
outputs/tables/rnn_history_*.rds
outputs/tables/rnn_metrics_*.json

# AutoML artifacts (leaderboard, metrics, predictions, etc.)
outputs/tables/automl_leaderboard_*.rds
outputs/tables/automl_preds_*.rds
outputs/tables/automl_metrics_*.json

# TSfeatures artifacts (rebuildable from 04_tsfeatures_build.R)
outputs/tables/fish_*_tsfeat*.rds


# generated ROC images (not tracked)
figures/roc_*.png

# --- renv local libraries & cache ---
renv/library/
renv/cache/
.Rprofile.user


# Python Env Files
.venv/
**/.virtualenvs/
**/r-keras/
**/r-keras/**

# --- OS/editor cruft ---
.DS_Store
Thumbs.db
/.quarto/

# R session cruft
.Rhistory
.RData
.Rproj.user/
*.Rproj

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Project outputs (generated)
outputs/

# Local deliverables (kits/zips, not source)
deliverables/

# Local data (inputs, scored outputs)
data/

# H2O logs (just in case you ever save any)
*.out
*.err

# Templates Folder
templates/

**/*.quarto_ipynb

# Quarto build artifacts
reports/**/_freeze/
reports/**/_cache/
reports/**/cache/
reports/**/figure-html/
reports/**/figure-pdf/
reports/**/*-cache/
*.aux
*.log
*.out
*.toc

# Ignore all PDFs in reports...
reports/*.pdf
# ...except the final report PDF you want to track
!reports/ETC5543_FishHydroacoustics_Report.pdf

# Local helper scripts (not required to knit the report)
Analysis/make_results_from_manifest.R
Analysis/utils_latest_artifacts.R

# build artefacts
reports/figures/
!reports/figures/.gitkeep
reports/figures_roc/
!reports/figures_roc/.gitkeep

# general outputs (already reproducible)
outputs/**
!outputs/tables/*.csv
!outputs/tables/*.rds

# Generated figures
reports/figures/*.png
!reports/figures/hydro.png      
reports/figures/.gitkeep

# ROC figures: only keep PNGs (unignore dir, then whitelist files)
!reports/figures_roc/
reports/figures_roc/*
!reports/figures_roc/*.png
!reports/figures_roc/.gitkeep

figures/roc_*_*.png

